<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SSO</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            text-align: center;
            padding: 50px;
        }

        a {
            display: inline-block;
            padding: 10px 20px;
            margin: 10px;
            text-decoration: none;
            color: #fff;
            background-color: #007bff;
            border-radius: 5px;
        }

        a:hover {
            background-color: #0056b3;
        }

        .profile {
            margin-top: 20px;
        }
    </style>
</head>

<body>
    <h1>Single Sign-On (SSO)</h1>
    <% if (user) { %>
        <div class="profile">
            <h2>Welcome, <%= user.displayName %>
            </h2>
            <img src="<%= user._json.picture %>" alt="Profile Picture" style="border-radius: 50%; width: 100px;">
            <br>
            <a href="/logout">Logout</a>
        </div>
        <% } else { %>
            <p>Sign in using google</p>
            <a href="/auth/google">Sign In with Google</a>   
            <% } %>
</body>

</html>